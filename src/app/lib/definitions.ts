export interface NavBarRoute {
  id: string,
  label: string,
  url: string,
  color: string,
  background: string
}
export interface SocialItem {
  url: string,
  icon: HTMLElement
}
export interface BlogCards {
  url: string,
  read: string,
  title: string,
  description: string,
}